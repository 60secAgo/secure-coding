# Tiny Second-hand Shopping Platform 개발 보고서

과목: 시큐어코딩
과제: Tiny Second-hand Shopping Platform

---

## 1. 개요

본 보고서는 강의자료에서 제시된 소프트웨어 개발 주기(요구사항 도출 → 시스템 설계 → 시스템 구현 → 테스팅 → 유지보수)에 따라, 기본 제공된 저장소(`github.com/ugonfor/secure-coding`)의 중고거래 플랫폼(`Tiny Second-hand Shopping Platform`)에 누락된 기능을 추가하고, 코드 전반의 보안 취약점을 점검·수정한 과정을 정리한다.

---

## 2. 요구사항 분석

### 2.1 기능 요구사항 (강의자료 21p, 35p 기준)

| # | 요구사항 | 최초 제공 코드 상태 | 본 과제에서의 처리 |
|---|---|---|---|
| 1 | 회원가입/로그인 | 구현됨 (평문 비밀번호) | 비밀번호 해시 저장으로 보완 |
| 2 | 상품 등록/조회 | 구현됨 (검증 없음) | 입력 검증 추가 |
| 2-1 | 상품 수정/삭제(소유자 확인) | 미구현 | 소유자만 수정, 소유자·관리자만 삭제하도록 신규 구현 |
| 2-2 | 상품 사진(이미지) | 미구현 | 이미지 업로드/표시 신규 구현(형식·크기 검증 포함) |
| 2-3 | 내가 등록한 상품 관리 | 미구현 | 마이페이지에 내 상품 목록 및 수정/삭제 링크 제공 |
| 2-4 | 사용자 조회(다른 유저 프로필) | 미구현 | `/user/<username>` 프로필 페이지 신규 구현 |
| 3 | 사용자 간 소통(채팅) | 전체 채팅만 구현 | 1:1 채팅 신규 구현 |
| 4 | 악성 유저/상품 차단 | 신고 접수만 구현 | 임계치 기반 자동 차단/휴면 구현 |
| 5 | 사용자 간 송금 | 미구현 | 신규 구현 |
| 6 | 상품 검색 | 미구현 | 신규 구현 |
| 7 | 관리자 전체 관리 | 미구현 | 관리자 역할/페이지 신규 구현 |

### 2.2 비기능 요구사항

- 보안: `secure_coding_checklist.csv`에 명시된 항목(입력 검증, CSRF, 비밀번호 해시, 세션 보안, Rate Limiting, SQL Injection 방지, 보안 헤더 등)을 최대한 충족.
- 사용성: 기존 Notion 스타일 UI를 유지하며 최소한의 폼/뱃지 UI 추가.
- 유지보수성: 정책 값(임계치, 길이 제한 등)을 상수로 분리하여 코드 상단에서 관리.

---

## 3. 시스템 설계

### 3.1 웹페이지 구성

강의자료 27p에 명세된 기본 페이지(회원가입/로그인/전체 상품조회 및 채팅/새 상품 등록/상품 상세조회/신고)를 모두 구현했으며, 그 외에 아래 페이지를 추가했다.

- `/profile` — 마이페이지 (소개글·비밀번호 수정, 내가 등록한 상품 관리)
- `/user/<username>` — 다른 사용자 프로필 조회(사용자 조회 기능) 및 해당 사용자의 상품 목록
- `/product/<id>/edit`, `/product/<id>/delete` — 상품 수정/삭제 (소유자·관리자 권한 확인)
- `/transfer` — 송금 및 송금 내역 조회
- `/chat/<username>` — 1:1 채팅방
- `/admin` — 관리자 대시보드 (사용자/상품/신고 관리)
- `/dashboard?q=` — 상품 검색 (기존 대시보드에 검색창 통합)
- `error.html` — 400/403/404/413/500 공통 에러 페이지 (내부 정보 비노출)

상품 목록(대시보드)에서는 강의자료 24p 명세대로 **상품명만** 노출하고, 클릭하면 상세 페이지에서 사진·설명·가격·판매자를 확인할 수 있도록 구성했다.

### 3.2 데이터베이스 설계

기존 `user`, `product`, `report` 테이블에 아래 컬럼/테이블을 추가했다.

**user**: `password_hash`(해시), `role`(user/admin), `status`(active/dormant/banned), `balance`(정수, 초기 10,000), `report_count`, `failed_attempts`, `lock_until`, `created_at`

**product**: `price`를 TEXT→INTEGER로 변경, `image_filename`(상품 사진 파일명), `status`(active/blocked), `report_count`, `created_at`

**report**: `target_type`(user/product) 추가, `(reporter_id, target_type, target_id)` UNIQUE 제약으로 중복 신고 방지

**transfer**(신규): `id, sender_id, receiver_id, amount, memo, created_at`

**message**(신규): 1:1 채팅 메시지 이력 저장용 `id, room, sender_id, content, created_at`

### 3.3 정책 값 (app.py 상단 상수)

- 로그인 실패 5회 → 5분 잠금 (`MAX_LOGIN_ATTEMPTS`, `LOGIN_LOCK_MINUTES`)
- 상품 신고 3회(서로 다른 신고자) → 자동 차단 (`PRODUCT_REPORT_THRESHOLD`)
- 사용자 신고 3회 → 자동 휴면 (`USER_REPORT_THRESHOLD`)
- 채팅 10초당 5개 메시지 제한 (`CHAT_RATE_WINDOW`, `CHAT_RATE_MAX_MSG`)
- 송금 1회 최대 10,000,000원 (`MAX_TRANSFER`)

---

## 4. 시스템 구현 요약

구현은 `app.py` 단일 모듈과 `templates/*.html`로 구성된다. 주요 구현 포인트는 다음과 같다.

- **인증/인가**: `login_required`, `admin_required` 데코레이터로 접근 제어를 라우트 단위로 명시.
- **CSRF 보호**: 세션에 저장된 토큰을 모든 상태 변경 폼에 hidden input으로 삽입하고, 서버에서 `secrets.compare_digest`로 검증(`validate_csrf`).
- **비밀번호 보안**: `werkzeug.security.generate_password_hash`/`check_password_hash` 사용(내부적으로 salted PBKDF2).
- **로그인 방어**: 계정별 실패 횟수·잠금시각을 DB에 저장하여 무차별 대입 공격 완화.
- **신고/차단 로직**: 신고 시 `DISTINCT reporter_id` 기준으로 누적 횟수를 집계하여 임계치 초과 시 상품 `status='blocked'`, 사용자 `status='dormant'`로 자동 전환.
- **송금**: 송금 직전 비밀번호 재확인(민감 작업 재인증) + `UPDATE ... WHERE balance >= ?` 조건부 갱신으로 잔액 음수화/이중 송금(레이스 컨디션)을 방지하고, 트랜잭션(`BEGIN IMMEDIATE` ~ `COMMIT`/`ROLLBACK`)으로 원자성 보장.
- **상품 사진**: 상품 등록/수정 시 이미지 업로드를 지원하며, 확장자 화이트리스트 + `imghdr` 시그니처 검증 + UUID 파일명 + 5MB 제한으로 악성 파일 업로드를 방어. 상세 페이지에서 사진을 표시.
- **사용자 조회/내 상품 관리**: `/user/<username>`에서 다른 사용자의 소개글과 활성 상품을 조회할 수 있고(비밀번호 등 민감정보는 렌더링하지 않음), 마이페이지에서는 본인이 등록한 상품 목록과 수정/삭제 링크를 제공.
- **검색**: `LIKE` 파라미터 바인딩으로 SQL Injection 없이 제목/설명 검색, LIKE 메타문자는 이스케이프, 차단된 상품은 결과에서 제외.
- **1:1 채팅**: Socket.IO room을 `sorted([uid1, uid2])`로 생성해 두 당사자만 알 수 있는 이름으로 구성하고, `join_direct_room`/`send_direct_message` 이벤트에서 세션의 사용자 ID가 방 이름에 포함되어 있는지 검증하여 임의의 방 참여(도청)를 차단.
- **관리자 페이지**: 역할이 `admin`인 계정만 접근 가능하며, 사용자 상태/상품 상태/신고 내역을 관리.
- **보안 헤더**: `after_request`에서 `X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`, `Content-Security-Policy`를 일괄 적용.
- **에러 처리**: 400/403/404/500에 대해 공통 에러 페이지를 렌더링하고, 서버 내부 오류는 `app.log`에만 기록하여 사용자에게 스택트레이스를 노출하지 않음.

---

## 5. 발견한 보안 약점과 수정 내역

원본 코드(`ugonfor/secure-coding` 초기 상태) 검토 결과 아래와 같은 보안 약점을 확인했고, 각 항목을 수정했다.

| # | 취약점 | 원본 코드의 문제 | 수정 내용 |
|---|---|---|---|
| 1 | 평문 비밀번호 저장 | `password` 컬럼에 사용자가 입력한 값을 그대로 저장/비교 | `password_hash` 컬럼으로 변경, `generate_password_hash`/`check_password_hash` 사용 |
| 2 | CSRF 미보호 | 모든 POST 폼에 토큰 없음 | 세션 기반 CSRF 토큰 발급 및 모든 상태 변경 요청에서 검증 |
| 3 | 하드코딩된 취약한 SECRET_KEY | `app.config['SECRET_KEY'] = 'secret!'` | 최초 실행 시 32바이트 난수를 생성해 `.secret_key`(gitignore 대상)에 저장, 환경변수로 재정의 가능 |
| 4 | 서버측 입력 검증 부재 | username/password/상품정보/신고사유 등 형식·길이 미검증 | 정규식·길이·범위 기반 검증 함수 추가(`is_valid_username`, `is_valid_password`, `parse_positive_int` 등) |
| 5 | 로그인 무차별 대입 공격 방어 없음 | 실패 횟수 제한 없음 | 5회 실패 시 5분 계정 잠금(`failed_attempts`, `lock_until`) |
| 6 | 계정 존재 여부 노출 | 아이디 오류/비밀번호 오류 메시지가 사실상 동일했지만 로직상 분기 여지 있음 | 아이디 불일치와 비밀번호 불일치 모두 동일한 일반 오류 메시지로 통일 |
| 7 | 세션 고정(Session Fixation) 위험 | 로그인 성공 시 기존 세션을 그대로 사용 | 로그인 성공 시 `session.clear()` 후 세션 재발급 |
| 8 | 세션 쿠키 보안 옵션 미설정 | `HttpOnly`/`SameSite`/`Secure` 미설정 | `HttpOnly=True`, `SameSite=Lax`, `Secure`(운영 환경) 적용, 세션 만료 30분 설정 |
| 9 | 보안 헤더 부재 | CSP/X-Frame-Options 등 없음 | `after_request`에서 일괄 적용 |
| 10 | 디버그 모드 상시 활성화 | `socketio.run(app, debug=True)` → 오류 시 내부 정보(스택트레이스) 노출 | 기본값 `debug=False`, 환경변수로만 활성화 가능, 공통 에러 페이지로 대체, 오류는 로그 파일에만 기록 |
| 11 | 채팅 인증/검증/속도 제한 부재 | 소켓 연결 시 로그인 여부 미확인, 메시지 길이 제한 없음, 클라이언트가 보낸 username을 그대로 신뢰(스푸핑 가능) | 연결 시 세션 인증 확인 후 미인증 연결 종료, 메시지 500자 제한, 서버가 세션의 사용자명을 사용(클라이언트 값 무시), 사용자별 10초당 5건 Rate Limiting 적용 |
| 12 | 1:1 채팅 미구현 | 전체 브로드캐스트만 존재 | Socket.IO room 기반 1:1 채팅 구현, 본인이 속하지 않은 방은 참여 불가하도록 서버측 검증 |
| 13 | 신고 남용 방지 없음 | 동일 대상 반복 신고 가능, 신고 누적에 따른 조치 없음 | `(reporter_id, target_type, target_id)` UNIQUE 제약으로 중복 신고 차단, 신고 누적 임계치 초과 시 상품 자동 차단·사용자 자동 휴면 처리 |
| 14 | 송금 기능 자체 미구현 | - | 잔액 컬럼 및 송금 라우트 신규 구현. 송금 전 비밀번호 재인증, 본인 송금 금지, 조건부 UPDATE로 잔액 음수화/레이스 컨디션 방지, 트랜잭션 원자성 보장, 송금 내역 로깅 |
| 15 | 상품 검색 기능 미구현 | - | 파라미터 바인딩된 `LIKE` 검색 신규 구현(제목/설명), 차단 상품은 검색 결과 제외 |
| 16 | 관리자 기능 미구현 | 관리자 개념 자체 없음 | `role='admin'` 도입, 기본 관리자 계정 자동 생성(최초 로그인 후 비밀번호 변경 안내), 관리자 전용 페이지에서 사용자 상태/상품 상태/신고 내역 관리 |
| 17 | 차단/휴면 계정 및 차단 상품에 대한 접근 제어 없음 | 상태 개념 자체가 없어 항상 접근 가능 | 로그인 시 `banned`/`dormant` 계정 로그인 차단, 차단된 상품은 소유자·관리자를 제외하고 404 처리(존재 여부 비노출) |
| 18 | 가격 필드 형식 검증 없음 | `price`가 TEXT로 저장되어 음수/비숫자/과도한 값 입력 가능 | 스키마를 INTEGER로 변경하고 1~100,000,000 범위의 정수만 허용 |
| 19 | (점검 결과 양호) SQL Injection | 원본 코드도 파라미터 바인딩(`?`) 사용 중이었음 | 신규 추가된 모든 쿼리(검색 LIKE, JOIN, 집계, 조건부 UPDATE 포함)도 동일하게 파라미터 바인딩만 사용하도록 유지·검증 |
| 20 | (점검 결과 양호) 반사/저장 XSS | Jinja2 autoescape가 기본 활성화되어 있어 `{{ }}` 출력은 자동 이스케이프됨 | `|safe` 필터 미사용을 전수 확인, 클라이언트 채팅 렌더링도 `innerHTML` 대신 `textContent`/`createTextNode`만 사용하도록 확인·수정 |
| 21 | 상품 수정/삭제 권한 통제 및 IDOR | 원본에는 수정/삭제 기능 자체가 없었음 | 수정/삭제 기능을 추가하되, 서버측에서 `seller_id`(소유자)를 검증하여 타인의 상품을 변경/삭제하는 IDOR(Insecure Direct Object Reference)을 차단. 삭제는 소유자 또는 관리자만 가능, 삭제 이력은 `app.log`에 기록 |
| 22 | 관리자 비밀번호 하드코딩 | 초기 개발 과정에서 기본 관리자 비밀번호를 소스에 상수로 두었음 | 소스에서 제거하고, 환경변수 `ADMIN_PASSWORD` 사용 또는 최초 실행 시 무작위 생성 후 콘솔에 1회 출력하도록 변경 |
| 23 | 로그인 계정 열거(타이밍) | 존재하지 않는 사용자는 즉시 반환되어 비밀번호 검증 경로보다 응답이 빨라 계정 존재 여부가 유추될 수 있었음 | 존재하지 않는 사용자에 대해서도 더미 해시 비교를 수행하여 응답 시간 차이를 완화 |
| 24 | 관리자 계정 상태 변경 보호 | 관리자 상태변경 라우트가 대상의 역할을 확인하지 않아, 조작된 요청으로 다른 관리자/본인을 잠글 여지가 있었음 | 대상이 `admin`이면 상태 변경을 거부하도록 서버측 가드 추가 |
| 25 | 교차 사이트 웹소켓 하이재킹(CSWSH) | 소켓 CORS 설정 필요 | Flask-SocketIO 기본값(동일 출처만 허용)을 사용하여 교차 출처 소켓 연결을 차단하면서 정상 브라우저 연결은 허용 |
| 26 | 악성 파일 업로드 | 상품 사진 기능 신규 추가에 따른 파일 업로드 위험 | ①확장자 화이트리스트(png/jpg/gif) ②매직 넘버(파일 시그니처) 직접 검증으로 위장 파일 차단 ③사용자 파일명을 신뢰하지 않고 UUID 기반 새 파일명 사용(경로 조작·덮어쓰기 방지) ④요청 본문 5MB 제한(`MAX_CONTENT_LENGTH`)으로 과대 업로드 차단, 초과 시 413 에러 페이지 |

---

## 6. 테스트

### 6.1 체크리스트 기반 점검

`secure_coding_checklist.csv`의 각 항목을 코드 리뷰로 점검한 결과는 아래와 같다(■: 충족, ▲: 부분 충족/트레이드오프, □: 미충족).

| 구분 | 점검 항목 | 결과 | 비고 |
|---|---|---|---|
| 회원가입/프로필 | 서버측 입력 검증 | ■ | 정규식 기반 검증 |
| | CSRF 보호 | ■ | 전 폼 토큰 검증 |
| | 비밀번호 해시 저장 | ■ | werkzeug PBKDF2 |
| | 세션 쿠키 설정 | ■ | HttpOnly/SameSite, Secure는 운영(HTTPS) 환경 한정 |
| | 세션 만료/재인증 | ■ | 30분 만료, 송금 시 비밀번호 재인증 |
| | 로그인 실패 방어 | ■ | 5회/5분 잠금 |
| | 오류 메시지 내부정보 비노출 | ■ | 공통 에러 페이지, 서버 로그 분리 |
| 상품 등록/관리 | 폼 입력 검증 | ■ | 제목/설명/가격 검증 |
| | XSS 방어 | ■ | autoescape + `|safe` 미사용 확인 |
| | 인증된 사용자만 등록 | ■ | `login_required` |
| | 소유자 확인 | ■ | 상품 수정은 소유자만, 삭제는 소유자·관리자만(`edit_product`/`delete_product`에서 `seller_id` 검증) |
| | 데이터 무결성 | ■ | 가격 정수/범위 검증 |
| 채팅 | 메시지 검증 | ■ | 길이 제한, 서버측 trim |
| | 사용자 인증 | ■ | 소켓 연결 시 세션 검사 |
| | 메시지 검증 | ■ | 서버가 형식/내용 검증 후 브로드캐스트 |
| | Rate Limiting | ■ | 10초당 5건 |
| | 연결 암호화(WSS) | ▲ | 개발 환경은 ws, 운영 배포 시 리버스 프록시+TLS로 wss 적용 필요(문서화만 완료) |
| 신고 | 폼 입력 검증 | ■ | 사유 500자 제한, 대상 유형 화이트리스트 |
| | 인증된 사용자 접근 | ■ | `login_required` |
| | 데이터 무결성/로그 | ■ | `app.log`에 신고 생성 기록 |
| | 신고 남용 방지 | ■ | UNIQUE 제약 + 임계치 자동 조치 |
| 전체 시스템 | ORM/파라미터 바인딩 | ▲ | ORM 대신 sqlite3 파라미터 바인딩으로 동등 수준의 Injection 방지(시간 관계상 SQLAlchemy 전면 도입은 보류, 향후 과제로 기록) |
| | DB 최소 권한 | ▲ | 개발환경 SQLite 특성상 OS 권한만 적용 가능, 운영 DB 전환 시 별도 계정/권한 분리 필요 |
| | 보안 헤더 | ■ | CSP/X-Frame-Options/X-Content-Type-Options 적용 |
| | HTTPS 적용 | ▲ | 개발환경은 HTTP, 운영 배포 가이드만 문서화 |
| | 에러/예외 처리 | ■ | 공통 에러 페이지 + 로그 분리 |
| | 라이브러리 최신화 | ▲ | 최초 설치 시점 최신 버전 사용 권장을 README에 명시, 지속 점검은 유지보수 단계 과제 |

### 6.2 기능 테스트 케이스

| ID | 시나리오 | 기대 결과 |
|---|---|---|
| TC-01 | 이미 존재하는 사용자명으로 회원가입 | "이미 존재하는 사용자명입니다" 안내, 가입 거부 |
| TC-02 | 8자 미만/숫자 없는 비밀번호로 회원가입 | 정책 위반 메시지, 가입 거부 |
| TC-03 | 잘못된 비밀번호로 5회 연속 로그인 시도 | 6번째 시도부터 5분간 잠금 메시지 |
| TC-04 | 차단(banned)/휴면(dormant) 계정으로 로그인 | 로그인 거부, 사유 안내 |
| TC-05 | 상품 등록 시 가격에 음수/문자 입력 | 등록 거부, 오류 메시지 |
| TC-06 | 소유자가 아닌 사용자가 차단된 상품 URL 직접 접근 | 404 처리 |
| TC-07 | 동일 상품을 동일 사용자가 2회 신고 | 두 번째 신고는 "이미 신고한 대상" 처리, 레코드 미생성 |
| TC-08 | 서로 다른 사용자 3명이 같은 상품 신고 | 상품 상태가 `blocked`로 자동 전환 |
| TC-09 | 보유 잔액을 초과하는 금액 송금 시도 | "잔액이 부족합니다" 안내, 송금 거부 |
| TC-10 | 송금 시 비밀번호를 틀리게 입력 | 송금 거부 |
| TC-11 | 관리자가 아닌 사용자가 `/admin` 접근 | 403 Forbidden |
| TC-12 | CSRF 토큰 없이 폼 데이터만 POST | 400 Bad Request |
| TC-13 | 로그인하지 않은 클라이언트가 소켓 연결 시도 | 서버가 즉시 연결 종료 |
| TC-14 | 10초 내 6개 이상 채팅 메시지 전송 | 초과분 거부 및 경고 메시지 수신 |
| TC-15 | 자신이 속하지 않은 1:1 채팅방 room 이름으로 join 시도 | 참여 거부(무응답) |
| TC-16 | 확장자만 `.png`로 바꾼 실행파일 등 위장 이미지 업로드 | `imghdr` 검증 실패로 업로드 거부 |
| TC-17 | 5MB 초과 이미지 업로드 | 413 에러 페이지 반환 |
| TC-18 | 정상 png/jpg/gif 사진과 함께 상품 등록 후 상세페이지 확인 | 상세페이지에 사진 표시 |
| TC-19 | 타인의 상품을 `/product/<id>/edit`로 직접 접근 | 403 Forbidden |
| TC-20 | `/user/<username>`로 다른 사용자 프로필 조회 | 소개글·해당 사용자 상품 목록 표시(민감정보 비노출) |

> **주의**: 본 세션에서는 실습 환경(에이전트 샌드박스)의 디스크 공간 부족으로 `python app.py` 실행 및 자동화된 테스트 수행이 불가능했다. 위 테스트 케이스는 코드 리뷰를 기반으로 기대 동작을 정리한 것이며, 제출 전 로컬 환경에서 직접 실행하여 위 시나리오를 재현·확인하는 것을 권장한다.

---

## 7. 유지보수 계획

1. 관리자 비밀번호는 소스코드에 두지 않고, 환경변수 `ADMIN_PASSWORD`로 지정하거나 최초 실행 시 콘솔에 출력되는 무작위 비밀번호를 사용한 뒤 로그인 후 즉시 변경한다.
2. `app.log`를 주기적으로 점검하여 로그인 실패, 신고, 송금, 관리자 조치 이력을 모니터링한다.
3. 운영 배포 시 HTTPS(리버스 프록시 + TLS 인증서)를 적용하고 `FLASK_ENV=production`으로 설정하여 `Secure` 쿠키 플래그가 실제로 동작하도록 한다.
4. Flask, Flask-SocketIO 등 의존 라이브러리의 보안 패치를 주기적으로 확인한다(`pip list --outdated`).
5. 서비스 확장 시 SQLite → SQLAlchemy/PostgreSQL 전환 및 DB 계정 최소 권한 분리를 검토한다.
6. 송금 기능은 현재 내부 가상 잔액(데모)이며, 실제 결제 연동으로 확장할 경우 PG사 연동, 전자금융거래법 등 별도 컴플라이언스 검토가 필요하다.
7. 사용자 피드백을 반영해 신고/차단 임계치, 채팅 Rate Limit 등 정책 값을 조정한다(코드 상단 상수로 분리되어 있어 수정이 용이함).

---

## 8. 결론

기본 제공된 코드는 기능적으로는 동작하지만 비밀번호 평문 저장, CSRF 미보호, 입력 검증 부재, 디버그 모드 노출 등 다수의 보안 약점을 가지고 있었으며, 요구사항 중 송금/검색/관리자/1:1 채팅/자동 차단 기능이 빠져 있었다. 본 과제를 통해 위 기능을 모두 구현하고, 발견된 보안 약점을 표 6과 같이 수정함으로써 시큐어코딩 원칙(입력 검증, 인증/인가, 최소 권한, 안전한 실패, 방어적 설계)을 실제 코드에 적용하는 경험을 할 수 있었다.
